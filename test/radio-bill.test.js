describe('The checkedRadioBtn function', function(){

    it('should return an amount of a call', function(){
         assert.equal(checkedRadioBtn());

        });

        it('should return an amount of an sms', function(){
          assert.equal(checkedRadioBtn());
      });
    
});
